<%= link_to 'New', new_campsite_path %>

<div class="row">
    <% @campsites.each do |campsite| %>
        <div class="col-sm-3">
            <div class="card">
                <div class="card-img-top">
                    <%= image_tag campsite.picture, width: '100%' %>
                </div>
                <div class="card-body">
                    <h6 class="card-title text-center"><%= campsite.name %></h6>
                    <h6 class="card_subtitle text-center"><%= campsite.location %></h6>
                    <div class="card-text">
                        <p><%= campsite.description %></p>
                    </div>
                    <div>
                        <%= link_to 'Edit', edit_campsite_path(campsite) %>
                        <%= link_to 'Show', campsite_path(campsite) %>
                    </div>
                </div>
            </div>
         </div>
    <% end %>
</div>

