<%= link_to 'New Order', new_order_path %>

<% @orders.each do |order| %>
    <div>
        <div>
            <%= order.street_address %> 
            <%= order.city %> 
            <%= order.state %>
            <%= order.zip %>
            <%= order.status %>
        </div>
    </div>
<% end %>